/*
 * ChartPanel.java
 *
 * Created on Feb 23, 2010, 11:05:27 AM
 */
package org.mei.securesim.platform.charts.ui;

import java.awt.Rectangle;
import org.mei.securesim.platform.charts.core.XYLineChart;
import java.awt.Graphics;
import java.awt.Image;
import javax.swing.JPanel;
import org.mei.securesim.platform.charts.IChartDisplay;

/**
 *
 * @author posilva
 */
public class ChartPanel extends JPanel implements IChartDisplay {
    private Graphics currentG;
    XYLineChart chart;
    public XYLineChart getChartObject() {
        return chart;
    }
    public ChartPanel() {
        initComponents();
        this.chart = new XYLineChart("Consumo de Energia", 5.0, "Energia consumida(%)", "Tempo(s)");
        this.chart.setSeriesLinesAndShapes("Consumo de Energia", true, false);
        //chart.getSeries("Consumo de Energia").add(1, 1);
    }

    /** This method is called from within the constructor to
     * initialize the form.
     * WARNING: Do NOT modify this code. The content of this method is
     * always regenerated by the Form Editor.
     */
    @SuppressWarnings("unchecked")
    // <editor-fold defaultstate="collapsed" desc="Generated Code">//GEN-BEGIN:initComponents
    private void initComponents() {

        setName("Form"); // NOI18N
        setLayout(new java.awt.BorderLayout());
    }// </editor-fold>//GEN-END:initComponents

    public Graphics getG() {
        return currentG;
    }

    @Override
    public void paintComponent(Graphics grphcs) {
       super.paintComponent(grphcs);
        Image offscreen = createImage(500,500);
        currentG = offscreen.getGraphics();
        chart.displayOn(this);
        grphcs.drawImage(offscreen,0,0,this);
    }

    public void update() {
        revalidate();
        repaint();
    }

    public void updateChart(double x, double y) {
        chart.getSeries("Consumo de Energia").add(x, y);
        update();
    }

    public Rectangle getRectangle() {
        return getBounds();
    }
    // Variables declaration - do not modify//GEN-BEGIN:variables
    // End of variables declaration//GEN-END:variables
}
